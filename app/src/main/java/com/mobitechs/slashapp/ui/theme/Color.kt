package com.mobitechs.slashapp.ui.theme

import androidx.compose.ui.graphics.Color


object SlashColors {
    // Main brand colors
    val Primary = Color(0xFF619958)  // Green button
    val Background = Color(0xFFF8F3DD)  // Cream background
    val Surface = Color(0xFF263238)  // Dark text

    // Input field colors (matching the pink input field in image)
    val InputBackground = Color(0xFFFFE4E4)  // Light pink input background
    val InputBackgroundError = Color(0xFFFFD6D6)  // Slightly darker pink for error
    val InputBorder = Color(0xFF9E9E9E)  // Gray border
    val InputBorderError = Color(0xFFE57373)  // Red border for error

    // Button colors (matching the muted green in image)
    val ButtonBackground = Color(0xFF619958)  // Muted sage green
    val ButtonDisabled = Color(0xFF9CAF88)  // Light gray for disabled
//    val ButtonDisabled = Color(0xFFD3D3D3)  // Light gray for disabled

    // Text colors
    val TextPrimary = Color(0xFF2D2D2D)  // Dark gray for main text
    val TextSecondary = Color(0xFF6B6B6B)  // Medium gray for subtitle
    val TextError = Color(0xFFE53E3E)  // Red for error messages
    val TextHint = Color(0xFF9E9E9E)  // Light gray for placeholders

    // Other colors
    val White = Color(0xFFFFFFFF)
    val CountryCodeBackground = Color(0xFFF0F0F0)  // Light gray for country code
    val Purple = Color(0xFF8B5CF6)  // Purple accent (for the M icon)
}